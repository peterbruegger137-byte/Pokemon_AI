package com.pokeai.monitoring

import org.springframework.data.repository.reactive.ReactiveCrudRepository
import org.springframework.stereotype.Repository

@Repository
interface StatsRepository : ReactiveCrudRepository<TrainingStats, Long> {
    // Hier erbt Kotlin alle Standardbefehle wie .save(), .findAll(), .delete()
    // Perfekt für Big Data: Alles passiert asynchron (reaktiv)
}